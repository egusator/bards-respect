name: bards
services:
  postgres:
    image: postgres
    container_name: bards-respect-database
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bardsdb
    ports:
      - "5432:5432"